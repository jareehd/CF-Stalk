import React from 'react';

export default (props) => {
    
    return (
        <div>
            p
                {/* props.RatingArray.map((e) => (
                    e
                )) */}
            
        </div>
    )
}